---
name: graph-memory
description: 本地知识图谱记忆系统。agent 通过 MCP 工具自动检索/写入/更新跨会话记忆。回答前先检索已有知识,回答后自动写入新知识。无需用户指令。
---

# Graph Memory — Agent 行为指令

## 核心规则(必须遵守)

**你是 agent,你有记忆。以下是你必须自动执行的行为,不需要用户指示。**

### 规则 1:回答前先检索

**每次回答用户问题前**,先调用 `retrieve` 工具检索已有知识:

```
retrieve(query: "用户问题的关键词", top_k: 5)
```

- 如果检索到相关知识:在回答中引用,避免重复解释
- 如果没检索到:正常回答,准备后续写入
- **不要告诉用户"我在检索记忆"** — 静默执行,直接给出答案

### 规则 2:回答后自动写入

**每次回答完用户后**,如果对话中产生了有价值知识,调用 `write` 工具写入:

```
write(content: "知识内容(保留技术细节)", title: "5-20字标题", node_type: "knowledge/preference/project/fact/skill/reference", source: "agent:对话主题")
```

**什么应该写入:**
- ✅ 技术原理/概念解释
- ✅ 项目信息/部署方案(服务器地址、端口、路径)
- ✅ 用户偏好/习惯
- ✅ 经验教训/踩坑记录
- ✅ 工具/命令用法

**什么不应该写入:**
- ❌ 寒暄/确认("好的""收到")
- ❌ 纯代码片段(没有解释的代码)
- ❌ 临时调试信息

### 规则 3:发现过时信息时更新

如果你在检索时发现图中的知识过时(如端口变了、路径换了),调 `update`:

```
update(query: "要更新的节点关键词", new_content: "更新后的完整内容", new_title: "更新后的标题")
```

### 规则 4:无需用户指令

**不需要用户说"记一下"或"存到记忆"** — 你自动判断内容是否有价值,有则写入。去重机制会自动处理重复。

## MCP 工具

| 工具 | 何时用 | 说明 |
|------|--------|------|
| `retrieve` | 回答前 | 检索已有知识(BM25+Embedding混合 → PPR多跳扩散 → MMR去重) |
| `write` | 回答后 | 写入新知识(自动建边+embedding去重) |
| `update` | 发现过时 | 修正已有节点(存历史,可追溯) |
| `extract` | 对话结束 | LLM提炼对话→结构化知识(批量,适合长对话) |
| `recent` | 偶尔 | 看最近加了什么(检查记忆质量) |
| `consolidate` | 发现重复 | 合并两个近似节点(边迁移,内容存历史) |
| `forget` | 定期维护 | 遗忘陈旧节点(>180天未访问+低频次) |

## 节点类型(6 种)

| 类型 | 用途 |
|------|------|
| knowledge | 技术原理/概念 |
| preference | 用户偏好/习惯 |
| project | 项目信息/部署 |
| fact | 环境/配置事实(端口、路径、版本) |
| skill | 技能/工具用法 |
| reference | 参考资料/经验教训 |

## 示例流程

```
用户: "8397板子怎么连?"

→ agent 自动调 retrieve("8397板子 连接")
→ 检索到: "8397板子三层架构:跳板机 ssh -p6022 lindu@144.168.58.199..."
→ agent 用检索到的知识回答用户

→ 回答后,agent 判断:用户可能有新的连接方式?
→ agent 自动调 write("8397板子连接方式(如有新信息)", "8397板子连接", "fact", "agent:8397连接")
→ 写入完成(静默,不告诉用户)
```

## 技术说明

- 检索:BM25(FTS5 trigram,中文友好) + Embedding(BGE-Small-ZH) → RRF 融合 → PPR 多跳扩散 → MMR 多样性
- 去重:content_hash 精确去重 + embedding 相似度 >0.85 → 返回已有节点(不静默丢内容)
- 图结构:节点间通过 cosine 相似度 >0.5 自动建边,PPR 沿边多跳传播
- 存储:SQLite(graph.db) + FTS5 全文索引 + embedding 持久化
- 本地运行,数据不出本机
